<UserControl x:Class="SpoofMess.Views.AuthorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpoofMess.Views"
             mc:Ignorable="d" 
             xmlns:uc="clr-namespace:SpoofMess.ViewElements"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <uc:InputTextBox Holder="Логин" Grid.ColumnSpan="2" Text="{Binding UserInfo.Login}"/>
        <uc:InputTextBox Holder="Пароль" Grid.ColumnSpan="2" Grid.Row="1" Text="{Binding UserInfo.Password}"/>
        <Button Content="Вход" Command="{Binding EnterCommand}" Grid.Column="1" Grid.Row="2" Style="{DynamicResource RightFunctionButton}" IsDefault="True"/>
        <uc:QuestionTextButton Margin="0 0 5 0" Prefix="Впервые в SpoofMess?" Grid.Column="0" Grid.Row="2" CommandText="Регистрация" Command="{Binding ChangeViewCommand}"/>
    </Grid>
</UserControl>
