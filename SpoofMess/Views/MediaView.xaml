<Window x:Class="SpoofMess.Views.MediaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpoofMess.Views"
        mc:Ignorable="d"
        Title="MediaView" x:Name="View" AllowsTransparency="True" Background="#55111111">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ContentPresenter Grid.RowSpan="2" Content="{Binding Content, ElementName=View}"/>
        <TextBlock Grid.Row="1"/>
    </Grid>
</Window>
