<UserControl x:Class="SpoofMess.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpoofMess.Views"
        xmlns:ve="clr-namespace:SpoofMess.ViewElements"
        xmlns:vm="clr-namespace:SpoofMess.ViewModels"
        mc:Ignorable="d" Height="450" Width="430" >
    <Grid >
        <Grid Width="430" Visibility="{Binding MainViewVisibility}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Border Padding="15 5 5 0">
                <DockPanel>
                    <Button Style="{DynamicResource MinimalButton}" Content="❌" DockPanel.Dock="Right" Command="{Binding CloseCommand}"/>
                    <Button Style="{DynamicResource MinimalButton}" Content="➖" DockPanel.Dock="Right"/>
                    <TextBlock Text="{DynamicResource Settings}" Style="{DynamicResource PrimaryText}" FontSize="25" FontWeight="Bold" HorizontalAlignment="Left"/>
                </DockPanel>
            </Border>
            <Border Padding="10 5" Grid.Row="1" Background="{StaticResource SecondWhite}">
                <Grid Height="auto">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <ContentControl Focusable="False">
                        <ContentControl.Style>
                            <Style TargetType="{x:Type ContentControl}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ContentControl}">
                                            <Grid>
                                                <Border x:Name="Mask" 
                                                        BorderThickness="1" 
                                                        CornerRadius="70" 
                                                        Background="White"
                                                        BorderBrush="Gray" />

                                                <Image Width="70" Height="70" Source="{Binding UserInfo.User.Avatar.Path}" Stretch="UniformToFill">
                                                    <Image.OpacityMask>
                                                        <VisualBrush Visual="{Binding ElementName=Mask}"/>
                                                    </Image.OpacityMask>
                                                </Image>
                                                <Button x:Name="SetAvatarButton" Visibility="Collapsed" Background="#AA333333" Content="📷" Padding="0 0 0 7" Style="{DynamicResource MinimalCircleButton}" Command="{Binding SetAvatarCommand}"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="SetAvatarButton" Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                    <StackPanel Margin="10 0" Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Style="{DynamicResource BaseText}" Text="{Binding UserInfo.User.Name}" FontWeight="DemiBold"/>
                        <TextBlock Style="{DynamicResource BaseText}" FontSize="16" Text="{Binding UserInfo.User.Login}"/>
                    </StackPanel>
                    <Button Focusable="False" Grid.Column="2" Padding="5" Style="{StaticResource MinimalButton}" Width="42" Height="42">
                        <Button.Content>
                            <Image>
                                <Image.Source>
                                    <BitmapImage UriSource="/Assets/qr.png" 
                                                 CacheOption="OnLoad" 
                                                 DecodePixelWidth="64"/>
                                </Image.Source>
                            </Image>
                        </Button.Content>
                    </Button>
                </Grid>
            </Border>
            <Border Grid.Row="3" Background="{StaticResource SecondWhite}" Padding="0 10">
                <StackPanel>
                    <ve:FunctionButton Text="{DynamicResource Profile}" Icon="👤" Alignment="Stretch" Command="{Binding ProfileCommand}"/>
                    <ve:FunctionButton Text="{DynamicResource NotificationsAndSounds}" Icon="🔔" Alignment="Stretch" Command="{Binding NotificationsCommand}"/>
                    <ve:FunctionButton Text="{DynamicResource PrivacySecurity}" Icon="🔒" Alignment="Stretch" Command="{Binding PrivacyCommand}"/>
                    <ve:FunctionButton Text="{DynamicResource Advanced}" Icon="🌡️" Alignment="Stretch" Command="{Binding AdvancedCommand}"/>
                    <ve:FunctionButton Text="{DynamicResource Language}" Icon="🈵" Alignment="Stretch" Command="{Binding LanguageCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
        <ContentPresenter Panel.ZIndex="22" HorizontalAlignment="Right" Content="{Binding AdditionalViewModel}">
            <ContentPresenter.Style>
                <Style TargetType="{x:Type ContentPresenter}">
                    <Setter Property="Width" Value="0"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DataContext.MainViewVisibility, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" Value="Collapsed">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation                 
                                             To="430"
                                             Storyboard.TargetProperty="Width"
                                             Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                                 To="0"
                                                 Storyboard.TargetProperty="Width"
                                                 Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseIn"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>

                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentPresenter.Style>
        </ContentPresenter>
    </Grid>
</UserControl>
