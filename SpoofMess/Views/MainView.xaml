<UserControl x:Class="SpoofMess.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpoofMess.Views"
        xmlns:ve="clr-namespace:SpoofMess.ViewElements"
        xmlns:filevm="clr-namespace:SpoofMess.ViewModels.FileViewModels"
        xmlns:vm="clr-namespace:SpoofMess.ViewModels"
        mc:Ignorable="d">
    <Grid Background="{DynamicResource ThirdWhite}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.45*" />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ve:SideMenu Background="{DynamicResource PrimaryWhite}" x:Name="SideMenu" IsOpen="{Binding SideMenuVisibility}" Panel.ZIndex="10" Grid.ColumnSpan="3">
            <StackPanel>
                <ve:FunctionButton Click="Menu_Item_Click" Text="{DynamicResource Profile}" Alignment="Stretch" Icon="👤" Command="{Binding ShowProfileCommand}"/>
                <ve:FunctionButton Click="Menu_Item_Click" Text="{DynamicResource NewGroup}" Alignment="Stretch" Icon="👥" Command="{Binding ShowCreateGroupCommand}"/>
                <ve:FunctionButton Click="Menu_Item_Click" Text="{DynamicResource Settings}" Alignment="Stretch" Icon="⚙️" Command="{Binding ShowSettingsCommand}"/>
            </StackPanel>
        </ve:SideMenu>
        <DockPanel>
            <DockPanel DockPanel.Dock="Top" Background="{DynamicResource SecondWhite}">
                <Button Focusable="False" Height="40" Width="40" Padding="0 0 0 5" VerticalAlignment="Center" DockPanel.Dock="Left" Content="≡" Style="{StaticResource MinimalCircleButton}" Click="Menu_Click"/>
                <ve:InputTextBox Holder="{DynamicResource SearchHolder}" DockPanel.Dock="Right"/>
            </DockPanel>
            <ListBox SnapsToDevicePixels="True" Background="{DynamicResource SecondWhite}" DockPanel.Dock="Bottom" ItemsSource="{Binding ChatsViewModel.Chats}" x:Name="ChatsList" SelectedItem="{Binding ChatsViewModel.SelectedChat}" MinWidth="100">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <Grid VerticalAlignment="Top" Grid.Column="0" Grid.RowSpan="2" Width="40" Height="40">
                                <Border x:Name="Mask" 
                                BorderThickness="1" 
                                CornerRadius="40" 
                                Background="White"
                                BorderBrush="Gray"/>
                                <Image Source="{Binding Avatar.Path}" Stretch="UniformToFill">
                                    <Image.OpacityMask>
                                        <VisualBrush Visual="{Binding ElementName=Mask}"/>
                                    </Image.OpacityMask>
                                </Image>
                            </Grid>
                            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Name}" FontSize="16" Foreground="{DynamicResource PrimaryBlack}" FontWeight="DemiBold"/>
                            <TextBlock Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Text="{Binding LastMessage.PreviewText}" Foreground="{DynamicResource PrimaryBlack}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="50"/>
                            <TextBlock Grid.Column="2" Grid.Row="0" Text="{Binding LastMessage.SentAt, StringFormat={}{0:HH:mm}}" Foreground="{DynamicResource SecondBlack}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.Template>
                    <ControlTemplate TargetType="ListBox">
                        <ScrollViewer CanContentScroll="False" Background="{DynamicResource SecondWhite}" HorizontalScrollBarVisibility="Disabled">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </ListBox.Template>
            </ListBox>
        </DockPanel>
        <GridSplitter Focusable="False" Grid.Column="1" Background="Transparent" Panel.ZIndex="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="2"/>
        <Border Background="{DynamicResource ThirdWhite}" Grid.Column="1" Grid.ColumnSpan="2">
            <ContentPresenter Content="{Binding MessagesViewModel}"/>
        </Border>
        <Grid x:Name="AdditionalContainer" Visibility="{Binding AdditionalVisibility}" Tag="{Binding AdditionalView, Mode=TwoWay}" Grid.ColumnSpan="3" Panel.ZIndex="10" MouseDown="Border_MouseDown" Background="#99000000">
            <Border x:Name="Container" MouseDown="ContentPresenter_MouseDown" Padding="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10" Background="{DynamicResource PrimaryWhite}" CornerRadius="10">
                <ContentPresenter MouseDown="ContentPresenter_MouseDown" Content="{Binding AdditionalView}">
                    <ContentPresenter.Resources>
                        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                            <local:SettingsView/>
                        </DataTemplate>
                    </ContentPresenter.Resources>
                </ContentPresenter>
            </Border>
        </Grid>
    </Grid>
</UserControl>
