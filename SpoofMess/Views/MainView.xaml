<Window x:Class="SpoofMess.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpoofMess.Views"
        xmlns:ve="clr-namespace:SpoofMess.ViewElements"
        xmlns:filevm="clr-namespace:SpoofMess.ViewModels.FileViewModels"
        xmlns:vm="clr-namespace:SpoofMess.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="1000">
    <Grid Background="{DynamicResource ThirdWhite}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.45*" />
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ve:SideMenu Background="{DynamicResource Primary}" x:Name="SideMenu" IsOpen="Collapsed" Panel.ZIndex="10" Grid.ColumnSpan="3">
            <StackPanel>
                <ve:FunctionButton Click="Menu_Item_Click" Text="Профиль" Alignment="Stretch" Icon="👤"/>
                <ve:FunctionButton Click="Menu_Item_Click" Text="Новая группа" Alignment="Stretch" Icon="👥" Command="{Binding ShowCreateGroupCommand}"/>
                <ve:FunctionButton Click="Menu_Item_Click" Text="Настройки" Alignment="Stretch" Icon="⚙️" Command="{Binding ShowSettingsCommand}"/>
            </StackPanel>
        </ve:SideMenu>
        <DockPanel>
            <DockPanel DockPanel.Dock="Top" Background="{DynamicResource SecondWhite}">
                <Button Height="40" Width="40" Padding="0 0 0 5" VerticalAlignment="Center" DockPanel.Dock="Left" Content="≡" Style="{StaticResource MinimalCircleButton}" Click="Menu_Click"/>
                <ve:InputTextBox Holder="Здесь искать" DockPanel.Dock="Right"/>
            </DockPanel>
            <ListBox Background="{DynamicResource SecondWhite}" DockPanel.Dock="Bottom" ItemsSource="{Binding Chats}" SelectedItem="{Binding SelectedChat}" MinWidth="100">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <Border VerticalAlignment="Top" Grid.Column="0" Grid.RowSpan="2" CornerRadius="40" Width="40" Height="40" BorderThickness="1" BorderBrush="Gray">
                                <Image/>
                            </Border>
                            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Name}" FontSize="16" Foreground="{DynamicResource PrimaryBlack}" FontWeight="DemiBold"/>
                            <TextBlock Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Text="{Binding LastMessage.Text}" Foreground="{DynamicResource PrimaryBlack}" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="50"/>
                            <TextBlock Grid.Column="2" Grid.Row="0" Text="{Binding LastMessage.SentAt, StringFormat={}{0:HH:mm}}" Foreground="{DynamicResource SecondBlack}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.Template>
                    <ControlTemplate TargetType="ListBox">
                        <ScrollViewer Background="{DynamicResource SecondWhite}" HorizontalScrollBarVisibility="Disabled">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </ControlTemplate>
                </ListBox.Template>
            </ListBox>
        </DockPanel>
        <GridSplitter Grid.Column="1" Background="Transparent" Panel.ZIndex="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="2"/>
        <Border Background="{DynamicResource ThirdWhite}" Grid.Column="1" Grid.ColumnSpan="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <ItemsControl Margin="2 0 0 0" ItemsSource="{Binding SelectedChat.Messages}" MinWidth="100">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid MinWidth="150" Margin="{StaticResource Margin1}" MaxWidth="500" HorizontalAlignment="Left">
                                <Grid.ContextMenu>
                                    <ContextMenu Style="{DynamicResource BaseContextMenu}">
                                        <MenuItem Header="Edit" Style="{DynamicResource MinimalMenuItem}"/>
                                        <MenuItem Header="Delete" Style="{DynamicResource MinimalMenuItem}"/>
                                        <MenuItem Header="Copy Text" Style="{DynamicResource MinimalMenuItem}"/>
                                    </ContextMenu>
                                </Grid.ContextMenu>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Column="0" VerticalAlignment="Top" Grid.RowSpan="2" CornerRadius="30" Width="30" Height="30" BorderThickness="1" BorderBrush="Gray">
                                    <Image/>
                                </Border>
                                <Border Padding="10 0" Grid.Column="1" CornerRadius="10" Background="{DynamicResource PrimaryWhite}" Margin="0 2">
                                    <Grid MinWidth="150" Margin="{StaticResource Margin1}" MaxWidth="500" HorizontalAlignment="Left">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition Height="auto"/>
                                            <RowDefinition Height="auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding User.Name}"/>
                                        <ItemsControl ItemsSource="{Binding Attachments}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                                            <ItemsControl.ItemContainerStyle>
                                                <Style TargetType="ContentPresenter">
                                                    <Setter Property="Margin" Value="0 2" />
                                                </Style>
                                            </ItemsControl.ItemContainerStyle>
                                            <ItemsControl.Resources>
                                                <DataTemplate DataType="{x:Type filevm:ImageViewModel}">
                                                    <ItemsControl ItemsSource="{Binding Files}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border  Margin="1" ClipToBounds="True" Background="{DynamicResource SecondWhite}" CornerRadius="4">
                                                                    <Image Stretch="UniformToFill">
                                                                        <Image.Source>
                                                                            <BitmapImage UriSource="{Binding Path}" 
                                 CacheOption="OnLoad" 
                                 DecodePixelWidth="400"/>
                                                                        </Image.Source>
                                                                    </Image>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <ve:ImagePanel MaxWidth="300"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                    </ItemsControl>
                                                </DataTemplate>
                                                <DataTemplate DataType="{x:Type filevm:MusicViewModel}">
                                                    <ItemsControl ItemsSource="{Binding Files}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="auto"/>
                                                                        <ColumnDefinition/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="auto"/>
                                                                        <RowDefinition Height="auto"/>
                                                                    </Grid.RowDefinitions>
                                                                    <Button Grid.RowSpan="2" Command="{Binding DataContext.PlayResumeCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" CommandParameter="{Binding}">
                                                                        <Button.Style>
                                                                            <Style BasedOn="{StaticResource CircleButton}" TargetType="{x:Type Button}">
                                                                                <Setter Property="Content">
                                                                                    <Setter.Value>
                                                                                        <Viewbox HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="Uniform" Margin="10.5 10 10 10">
                                                                                            <Path Fill="#AA000000" Data="M 0,0 H 7 V 20 H 0 Z M 13,0 H 20 V 20 H 13 Z" />
                                                                                        </Viewbox>
                                                                                    </Setter.Value>
                                                                                </Setter>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding DataContext.IsPlayeed, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" Value="False">
                                                                                        <Setter Property="Content">
                                                                                            <Setter.Value>
                                                                                                <Viewbox HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="Uniform" Margin="12 10 7 10">
                                                                                                    <Path Fill="#AA000000" Data="M 0,0 L 20,10 L 0,20 Z" />
                                                                                                </Viewbox>
                                                                                            </Setter.Value>
                                                                                        </Setter>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </Button.Style>
                                                                        <Button.Background>
                                                                            <ImageBrush Opacity="0.6"/>
                                                                        </Button.Background>
                                                                    </Button>
                                                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Name}"/>
                                                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PrettySize}"/>
                                                                </Grid>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </DataTemplate>
                                                <DataTemplate DataType="{x:Type filevm:FileViewModel}">
                                                    <ItemsControl ItemsSource="{Binding Files}">
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Grid MinWidth="150" Margin="{StaticResource Margin1}" MaxWidth="500" HorizontalAlignment="Left">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition/>
                                                                        <ColumnDefinition Width="auto"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition/>
                                                                        <RowDefinition Height="auto"/>
                                                                        <RowDefinition Height="auto"/>
                                                                    </Grid.RowDefinitions>
                                                                    <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Name}"/>
                                                                    <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding PrettySize}"/>
                                                                </Grid>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </DataTemplate>
                                            </ItemsControl.Resources>
                                        </ItemsControl>
                                        <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Height="auto" FontSize="14" Grid.Row="2" Grid.Column="0" Text="{Binding Text}" TextWrapping="WrapWithOverflow"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1" VerticalAlignment="Bottom" Foreground="{DynamicResource SecondBlack}" Text="{Binding SentAt, StringFormat={}{0:HH:mm}}"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer>
                                <ItemsPresenter />
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                </ItemsControl>
                <ItemsControl Grid.Row="1" ItemsSource="{Binding SelectedChat.CurrentMessage.Attachments}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding Files}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid MinWidth="150" Margin="{StaticResource Margin1}" MaxWidth="500" HorizontalAlignment="Left">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition/>
                                                <RowDefinition Height="auto"/>
                                                <RowDefinition Height="auto"/>
                                            </Grid.RowDefinitions>
                                            <Image Grid.ColumnSpan="2"/>
                                            <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Name}"/>
                                            <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding Size}"/>
                                            <Button Style="{StaticResource FunctionButton}" Content="❌" Command="{Binding DataContext.UnattachCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" CommandParameter="{Binding}" Grid.Column="1"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                </ItemsControl>
                <DockPanel Grid.Row="2" Background="{DynamicResource SecondWhite}">
                    <Button DockPanel.Dock="Right" VerticalAlignment="Bottom" Style="{StaticResource MinimalCircleButton}" Width="40" Height="40" IsDefault="True" Content="✉️" Command="{Binding SendCommand}"/>
                    <Button DockPanel.Dock="Right" Width="40" Height="40" VerticalAlignment="Bottom" Style="{StaticResource MinimalCircleButton}" Content="📎" Command="{Binding AttachCommand}"/>
                    <ve:InputTextBox MaxHeight="200" TextWrapping="Wrap" Holder="Здесь писать" Text="{Binding SelectedChat.CurrentMessage.Text}" AcceptsReturn="True"/>
                </DockPanel>
            </Grid>
        </Border>
        <Grid Visibility="Collapsed" x:Name="AdditionalContainer" Grid.ColumnSpan="3" Panel.ZIndex="10" MouseDown="Border_MouseDown" Background="#99000000">
            <Border x:Name="Container" MouseDown="ContentPresenter_MouseDown" Padding="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 10" Background="{DynamicResource ThirdWhite}" CornerRadius="10">
                <ContentPresenter MouseDown="ContentPresenter_MouseDown" Content="{Binding AdditionalView}">
                    <ContentPresenter.Resources>
                        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                            <local:SettingsView/>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type vm:CreateGroupViewModel}">
                            <local:CreateGroupView/>
                        </DataTemplate>
                    </ContentPresenter.Resources>
                </ContentPresenter>
            </Border>
        </Grid>
    </Grid>
</Window>
