<Window x:Class="SpoofMess.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpoofMess.Views"
        xmlns:uc="clr-namespace:SpoofMess.ViewElements"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <uc:SideMenu x:Name="SideMenu" IsOpen="Collapsed" Panel.ZIndex="10" Grid.ColumnSpan="3">
            <StackPanel>
                <uc:FunctionButton Text="Профиль" Icon="👤"/>
                <uc:FunctionButton Text="Новая группа" Icon="👥"/>
                <uc:FunctionButton Text="Настройки" Icon="⚙️"/>
                <uc:FunctionButton Text="Настройки" Icon="⚙️"/>
            </StackPanel>
        </uc:SideMenu>
        <DockPanel>
            <DockPanel DockPanel.Dock="Top">
                <Button DockPanel.Dock="Left" Content="≡" Style="{StaticResource FunctionButton}" Click="Menu_Click"/>
                <uc:InputTextBox Holder="Здесь искать" DockPanel.Dock="Right"/>
            </DockPanel>
            <ListBox DockPanel.Dock="Bottom" ItemsSource="{Binding Chats}" SelectedItem="{Binding SelectedChat}" MinWidth="100">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <Border Grid.Column="0" Grid.RowSpan="2" CornerRadius="40" Width="40" Height="40" BorderThickness="1" BorderBrush="Gray">
                                <Image/>
                            </Border>
                            <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Name}"/>
                            <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding LastMessage.Text}"/>
                            <TextBlock Grid.Column="2" Grid.Row="0" Text="{Binding LastMessage.SentAt, StringFormat={}{0:HH:mm}}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.Template>
                    <ControlTemplate TargetType="ListBox">
                        <ScrollViewer>
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </ControlTemplate>
                </ListBox.Template>
            </ListBox>
        </DockPanel>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="5"/>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <ItemsControl ItemsSource="{Binding SelectedChat.Messages}" MinWidth="100">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid MinWidth="150" Margin="{StaticResource Margin1}" MaxWidth="500" HorizontalAlignment="Left">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Border Grid.Column="0" Grid.RowSpan="2" CornerRadius="30" Width="30" Height="30" BorderThickness="1" BorderBrush="Gray">
                                <Image/>
                            </Border>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding User.Name}"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding User.Name}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Text}"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Foreground="Gray" Text="{Binding SentAt, StringFormat={}{0:HH:mm}}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.Template>
                    <ControlTemplate TargetType="ItemsControl">
                        <ScrollViewer>
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </ControlTemplate>
                </ItemsControl.Template>
            </ItemsControl>
            <DockPanel Grid.Row="1">
                <Button DockPanel.Dock="Right" Style="{StaticResource FunctionButton}" IsDefault="True" Content="✉️" Command="{Binding SendCommand}"/>
                <Button DockPanel.Dock="Right" Style="{StaticResource FunctionButton}" Content="📎" Command="{Binding AttachCommand}"/>
                <uc:InputTextBox Holder="Здесь писать" Text="{Binding SelectedChat.CurrentMessage.Text}"/>
            </DockPanel>
        </Grid>

    </Grid>
</Window>
